coverage:
  round: up
  precision: 5
  status:
    project:
      default:
        target: 50%
        threshold: 0%
      core-models:
        flags:
          - core-models
comment:
    layout: "reach, diff, flags, files"
ignore:
  - "deprecated"
