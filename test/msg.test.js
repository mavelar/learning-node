const sum = require('../dist/sum');
const msg = require('../dist/msg');

describe('/msg', () => {
    test('Hello World', () => {
        expect('Hello World');
    });
})

describe('/is_valid', () => {
    test('Is Valid', () => {
        expect(true);
    });
})